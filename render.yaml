services:
  - type: web
    name: warehouse-amr-fleet
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 main.py --mode dashboard
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
    healthCheckPath: /api/state
